profile:
  env: "dev"

# Redis 配置
redis:
  addr: "localhost:6379"     # Redis 服务器地址
  password: ""               # Redis 密码（如果没有设置可以留空）
  db: 0                      # 数据库编号（默认为 0）
  pool_size: 10              # 连接池大小（默认为 10）